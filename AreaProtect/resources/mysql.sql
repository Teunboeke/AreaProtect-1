CREATE TABLE IF NOT EXISTS areaprotect_data (
  owner VARCHAR(16),
  members VARCHAR(80),
  x1 INT,
  y1 INT,
  z1 INT,
  x2 INT,
  y2 INT,
  z2 INT,
  pvp TINYINT(1),
  build TINYINT(1),
  destroy TINYINT(1)
);
